package ninja.droiddojo.samples.conditions.`when`


fun main() {
    //Task: The Script will automatically generate numbers, Each number should be printed the following:
    //      - If the number is dividable by 3 the output should be "Fizz"
    //      - If the number is dividable by 5 the output should be "Buzz"
    //      - If the number is dividable by 3 and 5 the output should be "FizzBuzz"
    //      - Otherwise the original number should be printed

    //Hints:    - use a when statement
    //          - you can check if a number is dividable using the modulo (%) operator

    repeat(100) { number ->
        TODO("Format $number accordingly")
    }

}